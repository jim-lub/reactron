const channels = {
  window: {
    open              : "window:open",
    close             : "window:close",
    minimize          : "window:minimize",
    maximize          : "window:maximize",
  }
}

export default channels;
